namespace Models.Emun
{
    public enum GuildListSortField
    {
        id,
        name,
        topic,
        user_id,
        icon,
        notify_type,
        region,
        enable_open,
        open_id,
        default_channel_id,
        welcome_channel_id,
        boost_num,
        level
    }
}
